import React from 'react';
import Portada from '../../components/inicio/Portada';

const PagePortada = () => {
    return (
        <div>
            <Portada/>
        </div>
    );
};

export default PagePortada;